import {ReactElement} from 'react';
import {Text, View} from 'react-native';

const ExampleMolecule = (): ReactElement => (
  <View>
    <Text>ExampleMolecule</Text>
  </View>
);

export default ExampleMolecule;
