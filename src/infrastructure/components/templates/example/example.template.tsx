import type {ReactElement} from 'react';
import {Text, View} from 'react-native';

const ExampleTemplate = (): ReactElement => (
  <View>
    <Text>ExampleTemplate</Text>
  </View>
);

export default ExampleTemplate;
