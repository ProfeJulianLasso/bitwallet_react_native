import {ReactElement} from 'react';
import {Text, View} from 'react-native';

const ExampleAtom = (): ReactElement => (
  <View>
    <Text>ExampleAtom</Text>
  </View>
);

export default ExampleAtom;
